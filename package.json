{
  "name": "flux-core",
  "version": "0.0.1",
  "description": "Flux Player Core Components - Collections",
  "main": "dist/index.js",
  "repository": "https://github.com/flux-player/flux-collections.git",
  "author": "Melvin Mupondori",
  "license": "MIT",
  "types": "dist/types.d.ts",
  "prepublish": "tsc",
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit --coverage --verbose",
    "lint": "tsc --noEmit && eslint \"**/*.{js,ts}\" --quiet --fix",
    "watch-test": "npm run test -- --watchAll"
  },
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "@types/node": "^13.9.2",
    "@types/text-encoding": "^0.0.35",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0"
  },
  "dependencies": {
    "date-fns": "^2.11.0",
    "searchjs": "^1.0.2"
  }
}
